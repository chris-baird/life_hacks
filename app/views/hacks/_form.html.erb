<%= form_for @hack, html: { multipart: true } do |form| %>
  <%= form.label :name %>
  <%= form.text_field :name %><br>

  <%= form.label :body %>
  <%= form.text_area :body %><br>

  <%= form.file_field :image %><br><br>

  <%= form.submit 'Submit' %>
<% end %>

